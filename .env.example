# Development is the default. Use production only behind HTTPS.
JUNTO_ENV=development

# Required in production. Generate at least 32 random characters; never commit the real value.
JUNTO_SESSION_SECRET=

# Defaults to true in production and false in development.
JUNTO_SECURE_COOKIES=false
